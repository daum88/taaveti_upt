[project]
name = "taaveti-upt"
version = "0.1.0"
description = "Local single-user AI stock portfolio simulator"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "beautifulsoup4>=4.15,<5",
    "exchange-calendars>=4.11,<5",
    "fastapi>=0.138,<1",
    "lxml>=6.1,<7",
    "openai>=3.1,<4",
    "pandas>=3.0,<4",
    "python-dotenv>=1.2,<2",
    "requests>=2.34,<3",
    "rich>=15.0,<16",
    "uvicorn>=0.49,<1",
    "yfinance>=1.4,<2",
]

[dependency-groups]
dev = [
    "httpx2>=2.9,<3",
    "pytest>=9.1,<10",
    "pytest-playwright>=0.8,<1",
    "pytest-socket>=0.7,<1",
    "pytest-cov>=7.1,<8",
    "mypy>=2.3,<3",
    "ruff>=0.14,<1",
    "uv>=0.12,<1",
]
audit = [
    "pip-audit>=2.10,<3",
]

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
# SQL, prompts, and Rich markup contain long literals that Ruff cannot safely wrap.
# New application modules should still use the 120-column formatter limit.
ignore = ["E501"]

[tool.mypy]
files = ["domain", "application"]
follow_imports = "skip"
ignore_missing_imports = true
check_untyped_defs = true
disallow_any_generics = true
disallow_untyped_defs = true
warn_return_any = true
disable_error_code = ["import-untyped"]

[tool.uv]
package = false
