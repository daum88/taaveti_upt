"""External LLM completion adapters.

Each module here is a small true-external port over one completion transport so
that callers depend on a narrow surface rather than provider dictionaries and
back-imports.
"""
