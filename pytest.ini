[pytest]
testpaths = tests
pythonpath = .
addopts = -ra -m "not live" --disable-socket --allow-unix-socket
markers =
    live: requires external market-data or LLM services and is disabled by default
